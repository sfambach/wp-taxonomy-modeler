{
  "name": "wp-taxonomy-tree",
  "version": "0.0.270",
  "description": "WP Taxonomy Tree — admin tree scaffold + Gutenberg blocks",
  "private": true,
  "scripts": {
    "build": "wp-scripts build && node scripts/copy-block-json.js",
    "start": "wp-scripts start",
    "packages-update": "wp-scripts packages-update"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0"
  }
}
