> ## ⚠️ FROZEN — LEGACY DOCUMENT
>
> This file belongs to the **pre-2026-08-22 planning round** and is **no longer maintained**.
>
> - Do **not** edit it. Do **not** treat it as source of truth. Do **not** implement from it.
> - It is kept as a **quarry**: content reaches the new concept only through a reviewed
>   harvest sheet (see [`../NewConcept/README.md`](../NewConcept/README.md)).
> - Version numbers, `Q<n>` question ids, status flags and decision-log entries in here
>   describe the **old** model. They carry no authority over the new one.

# Open questions

> Resolve or defer during planning. Keep aligned with [`docs/plans/project-plan.md`](plans/project-plan.md) and [`docs/plans/planning-phase.md`](plans/planning-phase.md).

**Mode:** scaffolding + planning — answers become decision-log entries. Early scaffold may adopt a leaning (e.g. Admin-AJAX) without freezing the final product choice.

**Q123 doc pass (2026-08-09):** **closed** — OQ-W1…W16 decided. Canonical: [`docs/DEVELOPER-ATTRIBUTE-MODEL.md`](DEVELOPER-ATTRIBUTE-MODEL.md); log: [`docs/plans/q123-doc-pass-questions.md`](plans/q123-doc-pass-questions.md).

| ID | Question | Options | Current leaning | Status |
|----|----------|---------|-----------------|--------|
| Q1 | How should the admin UI talk to WordPress? | REST API / Admin-AJAX / both | **Decided (2026-08-08):** **Both.** Tree/admin primary = **Admin-AJAX**; **REST** for blocks / hosts (`wtt/v1/…`). | decided |
| Q2 | Which JS approach for the tree UI in MVP? | Vanilla JS / `@wordpress/scripts` + React | **Scaffold uses vanilla JS**; upgrade if complexity grows | open |
| Q3 | One tree screen for many taxonomies, or one screen per taxonomy? | Switcher on one screen / submenu per taxonomy | **Scaffold today: one product taxonomy (`wtt_fs`)** — no dual switcher. Future host registration may revive multi-tax UI | open |
| Q4 | Should activating the plugin replace core term list screens by default? | Opt-in per taxonomy / replace when registered / never replace | Opt-in when a taxonomy is registered with the environment | open |
| Q5 | Exact PHP namespace and prefix? | e.g. `WTT\` / `wtt_` | **Decided (2026-08-08):** Namespace **`WTT\`**, prefix **`wtt_`**, text domain **`wp-taxonomy-tree`**. | decided |
| Q6 | Minimum supported WordPress / PHP versions? | WP 6.x + PHP 8.x targets | PHP 8.x; modern WP — pin exact numbers at sign-off | open |
| Q7 | Is rename/reparent in-tree required for MVP? | Yes rename only / yes rename+reparent / later | **Decided (2026-08-08):** **Rename + reparent** required (scaffold already). | decided |
| Q8 | Placeholder right-hand panel in MVP? | Yes empty/host slot / tree-only until Phase 2 | Host slot preferred so electronic-parts can attach later | open |
| Q9 | When to integrate with `wp-electronic-parts`? | After MVP / after Phase 2 / never in-repo | After extension contract exists (Phase 2+) | open |
| Q10 | Packaging for reusable code? | Single plugin only / plugin + Composer package | Single plugin first | open |
| Q11 | How is Node stored? | Map 1:1 to WP terms / custom node table / hybrid | **Decided (2026-08-08):** **1:1 hierarchical WP terms** + `_wtt_*` term meta (scaffold SoT). Custom node table not planned for R1. | decided |
| Q12 | Which optional Node fields are in MVP? | slug / description / count / position / meta / short_description / icon | **description** required on every Node (may be empty); **short_description** decided; **icon** optional (**Q95** — `_wtt_icon`, default none); slug + count likely; **position** strongly needed if BOM/Recipe lines are Nodes (Q13/Q46) | open |
| Q13 | How are siblings ordered? | WP default name/term order / explicit position field | **Leaning: explicit `position` (or Relation order)** — BOM/Recipe line display needs stable sequence, not name sort | open |
| Q14 | Does a property slot have exactly one owning node? | Always one owning node / can be shared / taxonomy-level / other | **Superseded with Q64:** no Parameter class. **Eigenschaft** = typed **child Node** under exactly one parent (hierarchy `child_of`). | superseded |
| Q15 | Where are property-slot definitions stored? | Term meta / custom table / host plugin storage | **Superseded with Q64:** slots are **Nodes** (same persistence as Q11 — typically WP terms). Instance values still Q16/Q63. | superseded |
| Q16 | Are instance *values* (filled data) part of this plugin? | Yes in-core / host plugins only / later phase | **Decided (2026-08-08):** **In-core** — `Model_Data` / option `wtt_model_instances` (Q97). Host plugins may still own price/stock extras. | decided |
| Q17 | How does a Project get its trees (root nodes)? | Nodes carry `project_id` / project stores root ids / other | **Decided (domain model):** Project has `root_nodes` (list of Node). Persistence details still Q19. | decided |
| Q18 | How does Project relate to WordPress taxonomies? | One project = one taxonomy / project independent of taxonomy / hybrid | **Leaning strong: Project ≈ taxonomy** (practically the same); WP taxonomy slug on Project; Node has no taxonomy field | open |
| Q19 | Where is Project stored? | CPT / custom table / option / taxonomy | TBD — if Project ≈ taxonomy, storage may collapse toward the taxonomy (+ meta) or a thin Project wrapper (Q19) | open |
| Q20 | How are domain objects represented in PHP? | Typed DTO classes / arrays only / WP objects directly / hybrid | **Decided (revised Q64):** typed DTOs for Project, Node, Relation, Changelog, Change, …; **no Parameter / ParameterValue DTO**; services for behavior; no Tree/RootNode class | decided |
| Q21 | What is stored in Change.`change` (the Änderung)? | Plain text summary / structured field diff / both | Text summary first; structured diff optional later | open |
| Q22 | What is Change.`changer` (the Änderer)? | WP user ID / login / display name / Actor value object | WP user ID (+ display resolved in UI) leaning | open |
| Q23 | What format is Change.`version`? | Semver string / integer counter / object version snapshot | Align with plugin versioning where useful; decide later | open |
| Q24 | Which types require `prefix` and/or `base_unit`? | By type-node rules / flags / convention | **Decided via Q120 (2026-08-09):** **quantity** always has the fixed triple Value + Prefix? + Unit; Prefix optional per unit allowlist. Scalars do not. | decided |
| Q25 | How are Units represented? | Separate Unit / single unit Node / prefix+base | **Decided (Q51/Q75):** Basiseinheit unit = **`set`**; members via **`composition`** (Typ + optional Praefix + Kuerzel). Präfix from Definition tree; display Praefix+Kuerzel. | decided |
| Q26 | Must type/prefix/base_unit Nodes be children of Type/Präfix/Basiseinheit? | Strict branch check / any node | **Decided (reconcile Q77/Q92):** Catalog simples/units live under Type / Präfix / Basiseinheit anchors. **Type chooser** = **nodes** scoped by **`chooser_root` / bindings (Q92)** — no separate type-role flag. Präfix / Basiseinheit only under their anchors — never under Compositionen or Bauteile. Select nodes by **id** (not name). | decided |
| Q27 | How are type-Nodes organized? | Dedicated type tree in a project / flat list / convention | Example: Definition → Type | open |
| Q28 | Is a quantity unit prefix+base or one node (kOhm)? | prefix+base / single node | **Decided direction:** **prefix + base_unit** (e.g. k + Ohm) | decided |
| Q29 | Can prefix exist without base_unit (or vice versa)? | Both required together / either alone / type-dependent | Type-dependent leaning | open |
| Q30 | How are template trees applied to project-specific trees? | Deep copy / link / copy-on-write | Related to **Q50** (seed defaults via template-project copy) | open |
| Q31 | Does `Node.template` apply only to the root or inherit to descendants? | Root only / inherit | Root flag leaning | open |
| Q32 | Is the Definition tree itself a template? | Always template / never / optional | May be part of a **template Project** that is copied (Q50) rather than a separate “Definition template” flag alone | open |
| Q33 | How are named attributes (e.g. Wert on Widerstände) modeled? | Parameter class on Node / other | **Superseded (Q64):** **typed child Nodes** (Eigenschaften) with `type_id` → Typ-Ast. No Parameter class. | superseded |
| Q34 | How are special node *behaviors* encoded (not “what type am I”)? | PHP subclass / hard `kind` flag / **configuration** + Relations | **Decided (2026-08-08):** **Configuration** on the Node (+ Relations when the link is the structure). **No** PHP Node subclass per behavior; **no** hard `kind=simple`. Typing identity is **not** Q34 — see Q48 / Q88 / Q108 / **Q96** (`builtin.*` → term id). Confirmed by uniform-node + Registry-id + meta/config pattern. **Q49** = specialization / soft Relation lean on Simples (revised). | decided |
| Q35 | Do node–node links need typed edges with properties? | Plain parent/child only / kinds / full Relation + RelationType | Exploring RelationType pairs + display/inherit | open |
| Q36 | What is the core Type catalog? | Fixed list vs extensible | **Parked / superseded for Collection kinds by Q90 (2026-08-06).** Core catalog direction: **simples** + **quantity** / units + hierarchy (Q88) + attributes (Q87). Collection kinds `list` / `table` / `enum` are **not** active product types. Scaffold may still seed Complex leaves until a removal slice. | superseded |
| Q38 | Are single/multiple enum variants types or selection methods? | enum_single+enum_multiple types / one enum + selection_mode | **Parked with Q90** — catalog `enum` out of product direction; closed values via hierarchy / Festwerte / attributes instead. | superseded |
| Q37 | For `quantity`, is the numeric part `double`, `int`, or choosable per param? | Always double / always int / per-param `numeric_kind` | Per-param leaning | open |
| Q39 | Which scalar may enum option values use? | string only / any scalar / configurable | **Parked with Q90** — catalog enum out; reopen only if enum is revived. | deferred |
| Q40 | Parked: further **Node** idea from planning session | Resume when user returns to it | User asked to park mid-thought (“Knoten im Kopf”) — details TBD on resume | parked |
| Q41 | Bidirectional relations / inverse typing? | Separate inverse type / inverse field / reverse as view of same edge | **Leaning: no `inverse` field; one `label` per RelationType; reverse = view** | open |
| Q42 | How should related nodes be displayed per RelationType? | Always as tree children / type-specific (part-of as attributes, is_a as taxonomy, …) | **Leaning: part-of → attributes of parent**; is_a → taxonomy; uses → refs (`DisplayHint`) | open |
| Q43 | Can `consists_of` attributes be inherited along `is_a`? | No / copy / live inherit / merge+override | **Leaning: yes, inheritable**; mechanics TBD (related Q30) | open |
| Q44 | Does RelationType need **`directed`** (arrow vs line)? | Always directed / optional flag / derive from DisplayHint / drop | Tentative: directed → arrow `from→to`, else line; may overlap `bidirectional` — user unsure | open |
| Q45 | How is a quantity (Größe) bound when value sits on a Relation? | props `{value, prefix, unit}` / value on edge + unit **group** (prefix+unit) / Node only | **Leaning: Präfix+Einheit = group**; value often on edge; no loose value→prefix→unit chain | open |
| Q51 | How do Basiseinheit and Präfix relate, and where is the scale factor (×1000)? | Unit─[allows_prefix]→Präfix + multiplikator Relation / config on Präfix / factor only on allows_prefix edge | **Decided:** allowlist; multiplikator on Präfix (same SI exponents). **to_si** = Typ × multiplikator × `prefix_root_to_si`. Mass: SI base = **kg**, prefix root = **g** (`prefix_root_to_si=1e-3`). Scaffold: unit set = **Typ** + Praefix? + Kuerzel; metas `_wtt_multiplikator`, `_wtt_prefix_root_to_si`. **Vocab lock (2026-08-11):** Präfix/unit names, symbols, factors align **BIPM SI Brochure** + **ISO/IEC 80000** (binary prefixes = IEC 80000-13 only); allowlists/Q109/cross-UoM engines stay ours — [`attribute-choice-inheritance.md`](plans/attribute-choice-inheritance.md). **Catalog lock ≈ 0.0.540:** Konstanten/Präfixe host ChildList + Presentation/multiplikator attrs; leaves pico…Tera Compact; Choices exclude Centi. | decided |
| Q46 | Are domain structures (BOM, Recipe, …) hard classes or configurable Nodes? | Always host PHP classes / schema-as-Nodes templates / hybrid DTOs | **Decided (2026-08-08):** **No** BomList/Recipe/Build core classes — **Composition / Nodes** (Q56/Q85). | decided |
| Q47 | Where do value-shape rules live (e.g. BOM **Reference** = comma-separated RefDes list `R1,R2` / `C1…Cn`)? | Validator meta on the schema Node / Type (+ optional constraints) / slot payload / host-only | **Decided (2026-08-08):** Validators **0..n on the Node** (`_wtt_validators`); type defaults seeded create-time (Q71 snapshot — **not** live father-walk). Preferred render/converter same pattern. Registry bind for simples still **Q96**. | decided |
| Q48 | How are scalar data types configured and bound to slots? | Hardcoded-only catalog / **Nodes under Typ-Ast** + slot `type_id` / binding-only keys | **Decided lean (2026-08-08; typing SoT revised 2026-08-09):** Types = Typ-Ast **Nodes**; attribute type = **target of `besteht_aus` / `aggregation`** (Q108 superseded — no `attribute_typeof`). Registry chrome via **Q96** `builtin.*` → term **id**. Scaffold may still use interim `_wtt_type_id` on slots. | decided |
| Q49 | May simple data-type Nodes originate Relations, or must that be blocked? | Special Node kind that cannot build Relations / same Node + **config** that disables Relations from simples / allow Relations | **Revised decided (2026-08-08):** Same inheritance story as elsewhere — builtin Simples **may have `child_of` specialization children**. Purpose: **reusable named presets** (different validators, preferred converter already set e.g. Roman `int`, percent range on `double`) without redoing attribute Options everywhere. Child keeps the Simple **implementation** (Q96 ancestor / Registry); overrides **Config** (validators, preferred R/C, …). **Still lean (soft, no hard Capabilities required yet):** no attributes as host on Simples; no **outgoing** Relations from them. **Allowed:** `attribute_typeof` **target** (incl. specialized child). Prefer specialization node when reuse matters; attribute Options when one-off. | decided |
| Q50 | Where do default Nodes come from (Definitionsbaum anchors, fixed simples, …)? | **Generate** on Project create / **copy from a template Project** / hybrid | **Leaning: template Project** holds **simples** + **quantity** / units (Q90: not Collection enum/list/table); copy into new Projects (generate = fallback). Relates to Q30, Q32 | open |
| Q52 | How do **list** / **table** / **enum** relate (Collection model)? | Separate types / **Collection** super-kind / enum stays apart | **Superseded by Q90 (2026-08-06).** Prior decision (Collection → list\|table\|enum) is **parked** — not active product direction. Scaffold leftovers may remain until explicit removal. | superseded |
| Q53 | How is Collection **kind** bound for a concrete type (e.g. `my_list`, `Bauart`)? | Parent under list/table/enum / `type_id` → kind / XOR / other | **Parked with Q90** — Collection kinds out of product direction; reopen only if Q90 is reversed. | deferred |
| Q54 | How do tree hierarchy and Relations relate? | Semantic `parent_id` / org-only tree / hierarchy as Relation / hybrid | **Decided (2026-08-02; clarified 2026-08-06):** Hierarchy = protected Relation **`child_of`**. **`child_of` = inheritance / specialization only** (Q86/Q88 datatype = father; Q66 slot inherit along chain). **Not** for attributes — those use `besteht_aus` / `aggregation` (Q87). No dual writable `parent_id` SoT; WP `term_parent` may implement hierarchy edges. Other Relations additive. | decided |
| Q55 | How are catalog properties defined and inherited (e.g. Wert + Bauform on Widerstand)? | Typed child Nodes / only Relations `consists_of` | **Decided (Q64 superseded + Q66):** Eigenschaften = **typed child Nodes**. Descendants **inherit** slot defs along **`child_of`**. Instances fill values (Q16/Q63). E.g. Widerstand → Wert (`quantity`), Bauform (`Bauart` enum). | decided |
| Q56 | What is a Composition vs a catalog Bauteil? | GPU/Widerstand are Compositions / **only lists are Compositions** / host-only | **Decided lean (refined 2026-08-08; B6 chrome + UX):** **Bauteil** = catalog part under **Model**; **Composition** = Zusammenstellung. **BOM line Wert** types to **Model/Bauteil** (structure host), **not** catalog datatype `node_ref` / `node_embed`. Paint = preferred **`embed`**. **UX popup:** (A) TreeChooser kind under branch root only (parts: Bauteil); (B) same attribute Form as **filter** over Model_Data + **create** from same form if none — id on line (Q93). **Scaffold ≈ `0.0.384`:** Mult=`1` + Bauteil preferred `embed` + Phase A/B popup in object-render (Fill Model Data); Q107 server envelope still TODO. | decided |
| Q57 | Does a BOM / Composition have a Fußzeile (footer)? | No footer / host-only footer / **Composition has Fußzeile** | **Decided (refined):** Optional **Fuss** band on a `table`-typed node. Same field count as Zeile. Per Fuss **slot** meta `_wtt_footer_op` (`FooterAggOp`): `none` \| `text` \| `sum` \| `avg` \| `min` \| `max` \| `count`. Column type stays the Zeile value type (e.g. Menge=`int`); op is chosen on the Fuss field. Catalog: `Definition/Aggregate`. Defaults: `sum` for int/double, else `text`. Scaffold ≈ **0.0.192**. | decided |
| Q58 | How is BOM **Menge** expressed? | `quantity` with unit / free text / **Stück as `int`** | **Decided:** BOM Menge = **Stück** (piece count) via type **`int`** — not `quantity` (Größe). Display unit label „Stück“. Rezept-Menge may still be `quantity`. | decided |
| Q59 | Where is the Project **Startknoten** set? | Hardcoded root / first root_node / **Setup default** | **Decided:** **Startknoten** is set by default in **Project Setup** (`Project.start_node`). UI opens/focuses that node; may point at project root or a chosen branch (e.g. Typen, Compositionen). | decided |
| Q60 | Can a BOM restrict which types / Basiseinheiten are usable? | Always all project types / host filter / **per-Composition allowlists** | **Decided:** on a BOM/Composition one chooses **zulässige Typen** (subset under Type branch) and **zulässige Basiseinheiten** (subset under Basiseinheit). Empty allowlist = all under the project anchor. Same pattern for both. | decided |
| Q61 | How is a BOM named / titled? | Tree Node.name = project / **tree = structure name BOM**; project name at page fill | **Decided (refined — BOM composition):** Tree structure node stays **`BOM`**. **BOM** = `composition` of **Name** (text) + **Tabelle** (`type=table`). Name is **not** part of the `table` datatype. Instance Name filled on WP page/block. Title: **`BOM als Bauteilliste – {Name}`**. | decided |
| Q62 | How will frontend/editor expose a BOM table? | Hardcoded BOM UI / host-only / **Gutenberg block** | **Decided (direction; refined plan 0.7.49):** WP **blocks** as **views** (Q85): **`taxo/object-view`** = one structure host (+ optional Model_Data instance); **Taxo Table view** (`taxo/collection-table`) = all instances for a host. Catalog Collection `table` kind remains **Q90** parked — do not grow product Collection semantics via blocks. Tree = definition only (Q63). | decided |
| Q63 | What lives in the **tree (definition)** vs **WP page editor (instance values)**? | Everything in tree / everything in host / **split** | **Decided (refined Fallstudie):** **Tree** = definition — structure, typed property children, table bands via **`_wtt_prop_bindings`**, Fuss `_wtt_footer_op`, allowlists; legacy `slot_scope` where still used. **WP page/block** = **instance values** (Name, rows, cells). **Presentation** of schema + values uses the same object chrome in admin preview, block editor, and frontend (plan **0.7.49** parity) — not a separate block UI stack. | decided |
| Q64 | What is a **Parameter**? | Tree Node / vocab only / class with name+type | **Superseded (2026-08-02):** Parameter class **discarded**. Use typed child Nodes (Eigenschaften). See Q33 / Q66. | superseded |
| Q65 | How are files / images / links modeled as types? | Separate `url` / `file` / `image` / one **`media`** | **Decided:** one simple **`media`**. Value **MediaRef**: Library (`attachment_id`), URL-only (`url`), or **mirror** (`url` + `attachment_id`). Config: `allow_upload` (default on), `allow_url` (opt-in), **`allow_url_mirror`** (opt-in, default off). **`allowed_kinds`** (image…link): default **all off** — enable ≥1 kind. Display via MIME. No separate `image`/`file`/`url` types. | decided |
| Q66 | How do property definitions inherit along the tree? | No inherit / copy on create / **live inherit along parent_id** / merge+override | **Decided lean (refined 2026-08-11):** **live inherit** of attribute definitions along the **`child_of` chain** (Q54/Q87 Relation-only). Heir may override **without mutating the father’s edge**: Default (host name-map), **`choiceFilter`** / typeExtras (host map), RO, Hide; Type/Mult/Bindung → local shadow override. Diagram: [`docs/plans/attribute-choice-inheritance.md`](plans/attribute-choice-inheritance.md). Scaffold Choices-on-heir UI ≈ **0.0.455**. | decided |
| Q67 | When should a mirrored remote media file be re-fetched? | Once on save only / manual refresh / scheduled / on each read | **Open** — lock with Q65 mirror. Leaning: once on save + optional manual refresh; never silent re-fetch on every read. | open |
| Q68 | How do host plugins reuse / override MediaRef display (e.g. URL + Ampel)? | Rely on WP core filters only / WTT filter `wtt_render_media_ref` / always WTT chrome / hybrid | **Open — defer.** Goal: other plugins define richer URL/attachment chrome; WTT should pick it up. Today WTT uses custom `WTTMediaRender` (not auto). Resolve later with extension contract vs WP-standard hooks. | open |
| Q69 | How to handle Collection schema drift when page instance rows already exist? | Hard delete columns / soft-delete tombstone until force / block taxonomy edits | **Superseded for Model_Data by Q98 (UR-S1).** Legacy Collection/table row drift remains deferred. Model path: version stamp + orphan retention + conflict resolver (map / discard). | superseded |
| Q70 | How to separate Composition-wide properties (Name, Portionen, …) from table **columns** when both are child property slots? | Inherited-only = header / column folder Node / Relations for columns / **`slot_scope`** | **Decided (refined — bands + bindings):** **BOM** = Name + Tabelle via `composition` (Q61). Table columns = fields of the **bound Zeile** child. Band identity = **`_wtt_prop_bindings`** (type prop → child id), **not** the child’s display name. Optional Kopf/Fuss same field count. Legacy **`slot_scope`** where still used. Scaffold ≈ **0.0.181**. | decided |
| Q71 | Do settings on a data-type Node do anything for slots? | Ignore / live bind / **copy-on-assign presets** | **Superseded product (2026-08-09 OQ-W2):** **Hybrid** — live Settings from type/subtree; apply above **only if not overwritten** on the Relation capsule. Pure Q71 snapshot = scaffold debt, not product SoT. | decided |
| Q72 | What is `subtree` vs pick-and-fill? | Keep `subtree` / rename / split modes | **Superseded / deprecated (2026-08-08; hardened 2026-08-09):** Pick+fill = preferred render **`embed`** — **not** catalog types. **`node_embed`**, **`node_ref`**, RelationType **`ref_scope`**, and parent **`node_pick`** are **deprecated** (scaffold debt only). Alias `subtree` must not re-enter product. | superseded |
| Q73 | Shared settings for `node_embed` / `node_ref`; which catalog children are allowed? | Duplicate settings / **parent type `node_pick`** + allowlist | **Deprecated with Q72 (2026-08-09):** `ref_scope` + `allowed_ref_ids` / `node_pick` family are **not** product. Picker scope → Settings capsule / Q92 bindings when needed. Scaffold meta may linger until removal. | superseded |
| Q74 | How do admins create/delete non-hierarchy Relations (esp. **`composition`**) on a Node? | Only seed edges / custom UI per type / **reusable Relation picker** | **Decided (direction):** Reusable **Relation picker** (everywhere): (1) choose **RelationType** from Relationstypen-Ast, (2) choose target **Node** via tree picker. Modes **inline / popup** (default **inline**). Node detail: add/remove/duplicate/reorder outgoing Relations (not `child_of` — that stays reparent). Child Nodes **inherit** composition membership **definitions** along `child_of` where applicable (display/merge rules with Q66). **Scaffold ≈ 0.0.140:** `_wtt_relations` + edge ids + Add/Remove/Duplicate/Move UI. | decided |
| Q75 | What are **`set`** members — tree children or Relations? | Keep children (Q51 scaffold) / **`composition` Relations** | **Decided (refine Q51):** A Node typed **`set`** takes its **members from outgoing `composition` Relations** (targets), **not** from hierarchy children. Hierarchy (`child_of`) stays taxonomy/folder structure. Unit sets (Meter…): Typ / Praefix? / Kuerzel become **composition targets**. Preview/display still composes member values. **Scaffold ≈ 0.0.140:** members from composition; auto-migrate from children when no composition edges yet. | decided |
| Q76 | Does a Node’s **data type** inherit down the tree? | Never / always copy / **live inherit + override** / **superseded by Q88 for hierarchy** | **Superseded for hierarchy datatype by Q88 (2026-08-06).** Catalog-type **inheriting (vererbend)** + child **override** remains **scaffold interim** for Typ-Ast / assigned catalog types (e.g. `set` chains) where still wired. Product model for hierarchy nodes: datatype = **parent** only — no free override to a catalog type. Orthogonal to **Q66** slot-definition inherit. Exception `table never inherits` was a Q76 scaffold rule; hierarchy datatype under Q88 is always the parent Node. | superseded |
| Q77 | What is the **type chooser**; can a Typ-Ast Node also have a type? | Flat select / **tree type chooser** + Q92 scope | **Decided:** **Type chooser = nodes** (tree), scoped by **`chooser_root` / `chooser_focus` / catalog bindings (Q92)**. No type-role flag — pool membership = chooser scope only. Primary use: attribute / catalog field types. Hierarchy type = father (**Q88** / `_wtt_type_id`); free `set_type` dropped from admin (root seed-only). Catalog lock = **`_wtt_is_template`**. | decided |
| Q78 | Does a Relation need **multiplicity** (cardinality) in the definition tree? | None / on RelationType only / **on each Relation edge** | **Decided:** On each stored **Relation** edge (definition). Values: **`0..1`**, **`1`**, **`0..*`**, **`1..*`** (lower 0 or 1; upper 1 or *). Default **`0..*`**. **`child_of` always `1`** (exactly one parent; locked in UI / forced on write / repaired on read). **`ref_scope` stays `0..1`**. Not instance count — definition constraint. Distinct from Präfix **multiplikator**. **Scaffold ≈ 0.0.247.** | decided |
| Q79 | Are Node **names** unique? | Taxonomy-wide / siblings only / ID only / special rule for datatypes | **Revised (2026-08-07):** Identity / selection SoT = **`term_id` (ID)** always — **never select nodes by name**. Exceptions: **named config bindings** (constants → ids in `wtt_catalog_bindings` / similar). Instance names may collide across parents. Former rule “datatype display names unique taxonomy-wide” is **not** selection SoT (scaffold may still enforce — **debt** to drop or keep as optional UX only). WP sibling-name rules still apply. | decided |
| Q80 | How do validation **rules** relate to **bindings** and repairs? | Errors only / auto-mutate / **rules + optional fixes** | **Decided (direction):** **Bindings** (e.g. `_wtt_prop_bindings`) have **rules**. On failure: **0..n optional Fixes** (user-triggered; not mandatory). Example: Fuss count ≠ Zeile → “Create missing fields”. When adding a rule, decide/ask which fixes exist. Scaffold ≈ **0.0.188** (`fixes[]` + `wtt_fix_table_band_fields`). Rule: `.cursor/rules/bindings-rules-fixes.mdc`. | decided |
| Q81 | Must Kopf / Zeile / Fuss bindings point to **distinct** children? | Allow reuse / **unique per table** | **Deferred (UAT):** Today the same direct child can be bound to Zeile and Kopf. Unique bindings (Zeile ≠ Kopf ≠ Fuss) are a candidate rule + picker filter / unbind fix — implement only if UAT shows confusion. | deferred |
| Q82 | How are Fuss labels (“Summe”, “Gesamtpreis”) and computed aggregates made non-editable? | New type `label` / `text` + `editable` flag / **`footer_op` + existing `fixed`** | **Strong lean:** No new `label` type and no extra `editable` flag. (1) Aggregate ops (`sum`/`avg`/…) → **always read-only** (computed). (2) Static footer text → Fuss slot `footer_op=text` (or `none`) + **`fixed` literal** (e.g. “Summe”, “Gesamtpreis”) — same non-editable rule as fixed Zeile slots. (3) Editable Fuss text only if `text` and **not** fixed (rare). Example: Ref→fixed “Summe”; Menge→`sum`; Preis→`sum`; Kommentar→fixed “Gesamtpreis”. | open |
| Q83 | Bauteile: category/schema vs master data in one tree? | DigiKey-style mix / split Definition vs Implementation / **Model kinds + Implementation MPNs** | **Superseded (2026-08-08 OQ-B2):** **No Implementation/ branch** as product SoT. Kinds **and** part/MPN records live under **Model** (e.g. Model/Bauteil); address by **id** / bindings. BOM/line refs → **Model only**. No Lieferant / Bestellnummer / Hersteller on kinds. Dioden Arten = hierarchy under Model/Bauteil/Dioden (CatalogChoice). Scaffold **Implementation/** seeds = **debt** to remove/migrate. Prior “MPNs under Implementation” lean (2026-08-07) withdrawn. | superseded |
| Q84 | How should `node_ref` pick / create catalog targets in preview cells? | Select + checkbox wall / full tree picker / **catalog chooser** | **Deprecated with Q72/Q73 (2026-08-09):** `node_ref` / `ref_scope` create path is scaffold debt. Product pick(+fill) = preferred **`embed`** + composition/aggregation to Model hosts; chooser via Q92. | superseded |
| Q85 | Primary mental model: relations/table-grid vs object composition? | Relations-CRUD + Collection-table DB feel / **composition-first objects** | **Decided (2026-08-05, refined):** Objects + **`besteht_aus`**. Composition ≈ **class**; each member ≈ **attribute = Name + Typ** (`node.name` + `_wtt_type_id`). Platine/`BOM` examples unchanged. Table UI = view only. RelationType renamed **`besteht_aus`** (alias `composition`). Typing SoT = `_wtt_type_id` / Q88 only. Rule: `.cursor/rules/composition-first.mdc`. Plan **0.7.21**. | decided |
| Q86 | How does inheritance relate to **`child_of`**? | Separate `erbt_von` / only along `child_of` / both | **Decided (2026-08-06):** Inheritance = **`child_of` hierarchy only** (Q66 slots / Q88 hierarchy datatype). RelationType **`erbt_von` dropped**. Tree parent = single inheritance path. Plan **0.7.23**. | decided |
| Q87 | How are **Attributes** on a Node modeled (Name + Typ + Mult.)? | Typed hierarchy children / Relation props / **`besteht_aus` members** | **Decided product (2026-08-09 OQ-W1):** Attribute = **`besteht_aus` \| `aggregation` Relation only** — `name` + **target** (= type) + Settings capsule; **no slot term**. Never `child_of` to host. Scaffold `_wtt_attribute_slot` = migrate/remove debt. Inherit defs along host `child_of` (Q66 / OQ-W5). | decided |
| Q88 | What is the **data type of a hierarchy child**? | Own free type / inherit parent's catalog type_id (Q76) / **type_id = parent node** | **Decided:** **Everything via hierarchy.** Root is a node (Fallstudie). **Except on the root, effective type (`_wtt_type_id`) is always the father** (WP parent / `child_of`). **No Data type field/picker** in hierarchy node detail. Persist `type_id`=parent on create/reparent/repair; reads prefer derive-from-parent. **Free `set_type` dropped from admin** (including root). Root `type_id` → **Knoten** is **seed-only** (write by id in ensure/seed). Attribute members keep own catalog type (Q87). Scaffold ≈ **`0.0.330`**. | decided |
| Q89 | How does **node delete** work? | Hard delete / promote children / **soft-delete Trash** | **Decided (2026-08-06; refined Q97 2026-08-08; Q123 ≈ 0.0.420):** Soft-delete via Trash. **Cascade** = mark node + WP descendants `_wtt_trashed` (keep parent links). Also cascade **leftover attribute slot terms** (`toId` + `is_slot` only — never Relation edge UUID as term). Catalog type targets stay. **Model_Data** host bags soft-trash/restore/purge with composition children (Q111). **Promote** = reparent direct children to grandparent, then trash the node only (+ leftover slots + Model_Data). Empty Trash = permanent `wp_delete_term` (+ purge bags). Scaffold soft-delete ≈ **0.0.239**; attr cascade ≈ **0.0.370**; Q123-safe ≈ **0.0.420**. | decided |
| Q90 | Are Complex catalog kinds **enum** / **list** / **table** still in product? | Keep Collection model (Q52) / remove / **park** | **Decided (2026-08-06):** **Parked — out of product direction.** **enum** → hierarchy + attributes / Festwerte (CatalogChoice). **list** / **table** YAGNI for product core. **≈ 0.0.463:** Fallstudie soft-trashes `enum`/`list`/`node_pick`(+embed/ref); stops seeding them. Legacy **`table`** leaf kept only while BOM::Tabelle binds it. Bauart CatalogChoice under Complex. Agents **warn** before revive (`.cursor/rules/parked-complex-types.mdc`). **CatalogChoice UI:** type specialization depth ≤ 1 → flat select; ≥ 2 → tree chooser. Plan **0.7.122**. | decided |
| Q91 | Does a node-only domain imply **one** renderer? | Single paint path / **Registry + many type renderers** | **Decided (2026-08-06; parity note 0.7.49):** **No.** Domain objects are **nodes** (Q90 parks Collection kinds), but presentation stays **one Registry pipeline with many type-specific renderers** (simples `int`/`text`/… now; more when a type needs custom chrome). Contexts (`tree` / `form` / `table` / …) still differ. **Form(1 instance) / Table(n instances)** are presentation surfaces over a schema + values (`WTTObjectRender`) — not catalog `table`. That object chrome is shared **admin ↔ block editor ↔ frontend** (parity). Q90 does **not** collapse renderers into one class. Rule: `.cursor/rules/node-renderers.mdc`. Plan **0.7.27** / surfaces **0.7.49**. | decided |
| Q92 | How to address template catalog folders (Data Types / Simple) across installs if names change? | Hard-coded names / slugs / **config bindings by term id** | **Decided (2026-08-06; strengthened 2026-08-07):** Option **`wtt_catalog_bindings`** per taxonomy — **named config keys → term ids**. Attribute type picker: **`chooser_root`** (branch) + **`chooser_focus`**. Resolve **by id only**. **Never select nodes by name** in product logic; name lookup as runtime fallback is **debt to remove** (seed/migrate may still use names once to write bindings). Same rule for any special branch/leaf the product needs (quantity, legacy table, Model/Bauteil, …) — put the id in settings/bindings, do not re-find by name. Legacy helper keys `data_types` / `simple` / `complex` kept until cleaned. Scaffold ≈ **0.0.264** (`Catalog_Bindings`). | decided |
| Q93 | When CatalogChoice type host and/or selected child have attributes, what is stored? | Selected **node id** only / id + **instance values** on host attrs / on child attrs / on **both** (pick + fill) | **Decided (2026-08-08 OQ-R2b):** Host stores **id only**. Filled values live on the **referenced Model** instance (Q97). Preferred render **`embed`** = pick + compact fill chrome — **no** values bag on the host. Plan **0.7.52**. | decided |
| Q94 | What is the **data safety** strategy (backup / disaster recovery vs plugin export-import)? | Rely on full site/DB backup / native WXR Tools→Export / plugin JSON export-import / mix | **Decided for R1 (2026-08-08):** (1) **Primary DR** = full site/DB backup (+ uploads) — **not** a plugin Export button. (2) Native **WXR alone insufficient** (unbound taxonomy, Model_Data option, ID graphs). (3) Plugin **JSON export/import** = later product (copy tree); **MVP non-goal**. (4) Do **not** build admin Export now for “security.” | decided |
| Q95 | How do **optional tree icons** work (storage, allowlist, inheritance)? | Live father-walk / type→icon lookup table / **per-node meta + Settings allowlist + create-time standard-then-parent** | **Decided (2026-08-08; refined create priority; catalog cleanup; Identity vs Display UI):** Optional icon on each node (`_wtt_icon`; default empty). **Settings** curates allowlist (`wtt_tree_icon_keys` — Dashicons only). Admin detail: **Identity** (name / short description / description) vs **Display** (icon picker — key on node; allowlist external). **On create:** (1) if a **standard/default icon** exists for the new node’s name (`Tree_Icons::standard_for_name`), use it; (2) else copy parent icon if set. **Changing parent later does not update children**. **No** render-time father walk / name lookup / cascade. Tree paint: `Registry.renderTreeNode` shows icon **before** name. Seed: Simple → **`marker`**; known scalars via the same name→icon map. Former CSS examples `circle`/`dot` removed. Scaffold ≈ **`0.0.366`+**. Plan **0.7.46** / absorb **0.7.48**. | decided |
| Q96 | How do **Renderer / Converter / Validator** registries bind to a catalog node without `is_datatype`? | Term **name** = implementation key / explicit meta `_wtt_implementation_key` / Q92 bindings only / PHP “IntNode” subclass | **Decided (2026-08-08):** **Id bindings** in catalog config (extend **Q92** / `wtt_catalog_bindings`): keys such as **`builtin.int`**, `builtin.bool`, … → **term id**. Resolve paint/convert/validate: attribute/catalog type **term id** → binding key → Registry id (`int`, …). **Rename-safe.** New installs: seed writes ids into bindings. **Never** use display name as product SoT (name match = **debt** to remove). No PHP `IntNode` subclass. Preferred render/converter/validator metas may still store Registry **ids** (chrome choice); the **node↔builtin** link is the binding map. Composed Model hosts stay attribute-driven (not every host needs a builtin key). Scaffold ≈ **`0.0.385`** (`Catalog_Bindings::ensure_builtins` + reverse lookup). | decided |
| Q97 | Where do **Model_Data** instances live; how do parent/child instance deletes cascade? | Inline child blobs on parent / **per-model bags + instance links** / custom tables | **Decided (2026-08-08):** **Storage law:** always persist instance data under the **corresponding model object** (structure bag in `wtt_model_instances`). Parent **reads via relation** (`links[]`: `besteht_aus` \| `aggregation` → child structureId + instanceId) — **not** an inline `lines[]` payload on the parent. **Aggregation:** soft-delete father → children **remain**. **Composition (`besteht_aus`):** soft-delete father → composition-linked children soft-trashed with father; restore together; purge hard-deletes. **BOM:** Bauteilliste → Position lines = **composition** (exclusive); Taxo Table view = all instances of a host; BOM table UX = **related** lines of one list instance. Schema attribute slots still cascade with host Trash (Q89). Scaffold ≈ **0.0.370**. Plan **0.7.50**. | decided |
| Q98 | How do **model schema changes** stay consistent with stored **Model_Data**? | Silent drop / hard block edits / **version stamp + conflict resolver** | **Decided concept (2026-08-08 UR-S1):** Integer **Model version** (generation) on host; instances stamp `modelVersion`. **Bump only on structural** edits (add/remove attr, type, Mult, binding, duplicate). Cosmetic (rename, description, icon, preferred, Hide) → no bump. Warn popup on structural change **only if** host has instances (Settings `wtt_warn_structural_model_change`, default on). Display **Revision `G.e`** = `{modelVersion}.{editsSinceGeneration}` alongside Model version G (change log for Undo **parked**). Add field → empty on old rows. Remove → keep orphans; resolver: 1:1 map / regex+preview / discard (cross-model later). Type change → red **!** only if validators fail; click → Model versions for host. Mult 1→many auto-wrap; many→1 with 2+ values → resolver. Duplicate → bump; copy-data popup **only when data exists**. Cleanup v1 = conflict health line. Scaffold ≈ **0.0.376** (stamp/warn/badge; mapping DSL still TODO). Plan **0.7.52**. | decided |
| Q99 | How should **number / currency display** follow locale (decimals, thousands, later currency)? | WP locale / site language only / plugin config / hybrid | **Parked (2026-08-08) — later release.** Display of `double` / currency (and similar) must follow locale (EN: `.` decimal / `,` thousands; DE/EU opposite). Prefer WordPress locale / site language if that is the WP standard; plugin config only if insufficient. Out of European-first scope for now. **Not R1.** Assign to a release when open points are bucketed. | parked |
| Q100 | Multiple **Platine versions** / revisions vs one board ↔ one BOM? | Many versions per board / 1:1 only / version objects | **Parked (2026-08-08) — later release.** Real-world boards have revisions; **R1 stays 1:1** Platine ↔ Bauteilliste (Mult `0..1`). Do not implement versioned Platinen in R1. Assign when releases are bucketed. | parked |
| Q101 | Platine → Bauteilliste: **composition or aggregation**? | Composition (BOM dies with board) / **aggregation** (BOM survives) | **Decided for R1 (2026-08-08):** **Aggregation** Mult `0..1`. Authoring: create list without board; delete board → **list stays**. Composition would forbid orphan lists and cascade-delete the BOM — conflicts with flexible authoring. **Composition only** for Bauteilliste → Position lines (Q97). Narrative “BOM belongs to a board” is assignment, not exclusive ownership. | decided |
| Q102 | Composition **bottom-up**: may a child instance exist without a composition parent? | Allow orphans / block create / **allow + red badge until linked** | **Decided for R1 (2026-08-08):** Composition is exclusive ownership both ways in spirit: child **should** have a parent link. **Create via parent** (`create_linked`) is the happy path. If a child Model_Data exists without an incoming composition link → **invalid** → red **!** (same badge family as Q98 conflicts); click → repair/resolver (link to parent or discard). Do **not** silent-auto-create a parent BOM. Aggregation children (e.g. list without Platine) stay valid. Exact Fix actions when building (Bindings→Rules→Fixes). | decided |
| Q103 | When do **Gutenberg / frontend** Model_Data edits persist? | Always autosave / **block option auto vs button** / frontend editable | **Decided for R1 (2026-08-08; refined):** **Public frontend = display-only** — no planned visitor/reader data entry in R1 (comments etc. are WP core, not our Model_Data path). **Block editor** (wp-admin): per-block **Save mode** = `autosave` \| **`button`** (default **`button`**) for authors editing instances. Admin Fill Model Data / tree preview keep existing save habits. **Later release (parked):** visitor/contributor flows (e.g. suggest BOM improvements / alternate parts → review → accept into order list) — not designed yet; do not build for R1. | decided |
| Q104 | Separate **Mandatory** flag on composition/aggregation (node or edge)? | New flag / Mult / composition implies required | **Decided (2026-08-08):** **No separate Mandatory.** **Required fill = Mult** `1` or `1..*` (lower bound 1). Optional = `0..1` / `0..*`. Composition/aggregation = ownership/cascade only (Q97) — not “must fill”. | decided |
| Q105 | Attribute **Hide** vs node visibility — rename / SoT? | Host hide list / **node flag** / both | **Decided (2026-08-08; Mult narrowed; refined 2026-08-12):** **Background only (BO)** on the attribute (own Hide / `edge.hidden`). Intermediate/computed / calc constants — user does not see or enter. Admin/background still. Not tree-hide. **Invariant:** BO ⇒ Mult **single-valued** (`0..1` **or** `1`). Many (`0..*` / `1..*`) blocked. Comp/agg OK. **Validation (Bindings→Rules→Fixes ≈ 0.0.426; Mult gate ≈ 0.0.491):** if own BO and Mult is many → error + optional fixes: (1) `set_mult_01`, or (2) `clear_hide`. Either/or; user-triggered, never auto. Write path blocks illegal pairs. Inherited hide = cover-up (no Mult gate). | decided |
| Q106 | May Mult > 1 attributes have **multiple default values**? | One template only / **multiple defaults allowed** / edge-only list | **Decided (2026-08-08):** Defaults = **schema templates**, not live `Model_Data`. Always a **list** sized by Mult: `0`/`1` → 0..1 entry; `0..*`/`1..*` → 0..n. **Scalars** (text/int/…): list of values (e.g. `Herbert`; or several strings). **Related / composition Mult** (e.g. Bauteilliste→Position): list of **nested value maps** (default lines) — edit with same Form/Table chrome, stored as templates on the attribute. **On create** of a parent instance: materialize templates into instance values / `create_linked` children. **Not** stamped as real instances under the schema host (no counts/conflicts for defaults). **Delete attribute** → templates go with the slot/host map cleanup (already clears `_wtt_attribute_fixed_values`); no orphan-default complaints. **Change defaults** when instances exist → existing rows keep data; only **new** creates get new seeds; **no** model-version bump (not structural — Q98). Structural attr edits still Q98. **SoT:** prefer **slot** list meta (align RO/BO); scaffold host map `_wtt_attribute_fixed_values` is interim (string\|list\|nested maps). Scaffold seed path ≈ **`0.0.383`** (full scalar list on create/open-new/fill-samples; related default-row admin UI TODO). | decided |
| Q107 | Validation **severity**: error vs warning? | Errors only / **error + warning** / always block | **Decided (2026-08-08):** One Q80 envelope `{ ok, errors[], warnings[], fixes[] }`. **Error** = illegal (red !); **Warning** = soft/amber. Auto-run fixes never. **Save with warnings:** always allowed. Optional **dialog on warnings:** Settings `wtt_dialog_on_validation_warnings` (**default OFF**) under **Confirm dialogs** (with `wtt_warn_structural_model_change` default ON + confirm-node-delete). **Save with errors by context:** data entry (Fill Model Data / Gutenberg) **allow** + keep invalid badge; model/schema admin (Taxonomy Tree definition) **block** illegal schema writes. First concrete warning (e.g. aggregation unassigned) may stay stubbed until a consumer exists. Scaffold ≈ **`0.0.382`**. | decided |
| Q108 | Attribute / field **typing SoT** vs tree **display** of children? | `child_of` type / `attribute_typeof` / **composition target** | **Superseded lean (2026-08-09):** Typing = **target of `besteht_aus` / `aggregation`** (direct → type node, e.g. `size`). **No RelationType `attribute_typeof`.** Type Settings → **Settings capsule on that Relation**. Tree display of aux nodes = orthogonal (parked with aux-node path). Scaffold may still use `_wtt_type_id` on slots until migrate. | superseded |
| Q109 | How do **measure / quantity** values work (Typ + Präfix + Basiseinheit), and what happens on **unit/prefix switch**? | Store display triple only / always store SI canonical / rescale magnitude on switch / switch unit only keep digits | **Decided (2026-08-08):** Measure/quantity stays the composed reading **Typ + optional Präfix + Basiseinheit** (Q25/Q28/Q51/Q75) — not a Simple with validators (Q49). **Storage SoT = display triple** (magnitude + prefix id + base unit id); no mandatory SI-only store for R1. **On Präfix switch within the same Basiseinheit:** **rescale Typ** so the **physical quantity stays the same** (use Präfix multiplikator / `to_si` from Q51). Example: `1` + (none) + Ohm → Milli → Typ becomes `1000` + Milli + Ohm. **Digits-only switch** (keep `1`, only change label) is **not** the product default — that would change meaning. **Cross-Basiseinheit** (Ohm↔Farad etc.): **not** a silent unit switch — refuse / separate type change. Same-dimension only. **Not currency** — FX is **Q110**. **Scaffold ≈ 0.0.386:** `WTTConverter.Quantity` rescale helper wired on quantity Präfix change (node-render + join-units). | decided |
| Q110 | How does **currency / money** switch and convert (e.g. EUR → USD)? | Same as Q109 Präfix rescale / amount+currency composed + **FX rates** / store one canonical currency | **Parked (2026-08-08) — hold for now.** Distinct from measure (Q109) and from **same-currency subunit scale** (**Q119**). Money FX (Euro→Dollar) needs **exchange-rate conversion**, not multiplikator/`to_si`. Rates are **time-varying external data**. Related **Q99**. Not R1. | parked |
| Q111 | When are attribute values **inline** on the host vs **linked** Model_Data instances? | Always linked (Q97) / always inline blobs / **derived from type** / new is_datatype-like flag / **Bindung** | **Revised decided (2026-08-09):** **Bindung = storage SoT.** **Composition (`besteht_aus`)** → embedded in parent (exception: **Bauteilliste→Position** list lines). **Aggregation** → linked Model_Data. **Rule of thumb:** under **Model**, default **aggregation**; only list **Position** = composition. Builtin Simples/`quantity` = host-field shaped. Scaffold ≈ **0.0.388** (`Attribute::DEFAULT_BINDING` = aggregation; seed/ensure repair). Preferred **`MultistepRenderer`** = UI (Q113 / Q112 revised). | decided |
| Q112 | Rename Preferred render **`embed`** (pick + fill)? | Keep `embed` / rename / drop as layout | **Revised (2026-08-14 ≈ 0.0.546 / Bindung chrome ≈ 0.0.552 / kind Preferred ≈ 0.0.554 / Composition Display ≈ 0.0.555):** wire id **`MultistepRenderer`** (UI **Multistep**); legacy aliases. Mode meta `_wtt_multistep_mode` = dialog\|inline (≠ Q74). **Bindung splits Phase B chrome (Q111):** **Composition** → multi-step kind pick + fill attrs with the **kind node’s Preferred** (e.g. Compact for BU leaves) — values in parent/context; Display shows kind Preferred with filled values (not “—”); soft Display refresh without Editable remount. **Aggregation** → kind pick + **chooser** (filter/search existing Model_Data + create + bind id — UR-B6); filter also uses kind Preferred. Compact Preferred: meta `_wtt_compact_show_labels` (Show field labels). | decided |
| Q113 | One **Renderer registry** for field + object layouts; how is Preferred **assigned/stored**; UI when only one choice? | Dual Preferred keys (form\|int) / unify Registry + **config on type** / keep derive-from-type-name | **Scaffold cutover (2026-08-09) ≈ `0.0.388`:** PHP **`enum Renderer: string`** + Preferred normalize (legacy aliases). Object layout wire ids `FormRenderer`…`MultistepRenderer` (ex-`EmbeddedRenderer`). Field paint ids still short (`int`, …). PHP **8.1+**. **Sole-option gray-out** generalized as **Q116** (not Preferred-only). | open |
| Q114 | Should **attribute** Options show the same Node chrome as detail Settings (Preferred R/C, Validators, Rules)? | Panel-only extras / **same Node config surface** / **recursive walk** | **Decided (2026-08-09 OQ-W6):** **Same job** — node detail and attribute Settings both use the **recursive walk** (node + all subnodes). Flat/single-node Options chrome superseded. | decided |
| Q115 | Align Settings **Fixed value** with attribute **Read-only** / **Default value**; store defaults on nodes? | Keep Fixed radios / **RO + Default everywhere** / defaults type-only | **Decided (2026-08-09):** **Read-only** is the only lock UI (Settings + Attributes). Show RO in Settings chrome; **gray** when not a fillable attribute slot (OQ-A3). **Default value** on **nodes** = same job as attribute Default (seed, not lock): keep `_wtt_fixed_*` as node seed SoT (UI renamed); attribute instance seeds stay host `_wtt_attribute_fixed_values` (Q106 → later slot meta). **Builtin Simple** catalog leaves: Default **grayed** (no preset on the leaf itself). **Specializations** / typed fields: Default editable. Scaffold ≈ **`0.0.390`**. | decided |
| Q116 | Required **list-select** with only one choice — auto-select and gray? | Always selectable / hide field / **auto + gray when required** | **Decided (2026-08-09):** General ListChooser rule. If the field **must** have a value (no zero-lower multiplicity / not optional) **and** there is **exactly one** real option → **select it and disable (gray)**. Several options → leave selectable. Optional (`0` / `0..1` / `0..*`) stays selectable even with one choice. Applies to Preferred render/converter, CatalogChoice flat selects, `renderOptionsSelect`, and similar list-selective controls. Scaffold ≈ **`0.0.392`**. | decided |
| Q117 | Where do **description / short description / icon** live — on the node or external presentation? | Keep on term / **presentation store** (locale texts + invariant icon) | **Decided (2026-08-09):** Presentation leaves the node identity surface. SoT = table `wtt_node_presentation` keyed by **term id + context + locale**. Text contexts: `form`, `table`, `select`, `symbol`, `help` (locale-aware). Context **`icon`** uses same store/technique but locale `*` (not translated). Migrate from term description / `_wtt_short_description` / `_wtt_icon`. Admin **Presentation** list (WP_List_Table, incomplete filter) before Settings; lazy foldable on tree detail (next). Scaffold store+list ≈ **`0.0.393`**. **Consumer type (≈ 0.0.490):** Simple datatype **`node_presentation`** (alias `display_node_name`) picks one context via type/attr option — e.g. With-prefix **Kuerzel** → `symbol`. | decided |
| Q118 | How is node detail grouped — identity vs display? | Flat form / **Node properties vs Display** | **Decided (2026-08-09):** **Node properties:** name, Flags, Read-only, Default value (+ type settings). **Display:** Presentation foldable (texts+icon), Preferred R/C, Validators. Remove description/short inputs from detail (use Presentation). | decided |
| Q119 | How are **money amounts** stored, entered, and displayed (same currency; cents vs euro)? | Hard-code Preis / money Simple type / **amount + converters + entry scale** / ISO minor only | **Decided lean (2026-08-09; ISO vocab lock 2026-08-11):** **Store** always precise in **currency major unit** (e.g. EUR). **Währung leaves align ISO 4217** field names: `currencyCode`, `currencyNumber`, `currencyExponent` (EUR→2 → Cent). ISO does **not** define rounding, FX, or store format — display rounding = **our** converter policy; FX = **Q110**. **Entry scale** major vs minor uses `currencyExponent`. Preis = composed Menge + Währung (+ optional later currency-scoped scale). Nothing from Q119/Q121 dropped. Detail: [`attribute-choice-inheritance.md`](plans/attribute-choice-inheritance.md) Application B. **FX = Q110** (parked). Parallel SI: Q109/Q51. | decided |
| Q120 | What **is Quantity** (anatomy + how units/prefixes/conversion rules hang together)? | Ad-hoc per field / hard-code Ohm\|Preis / **fixed triple + rule profiles on units** | **Decided lean (2026-08-09; refined same day):** **Quantity = always Value + Prefix? + Unit.** Catalog folders (**Basiseinheiten**, **Währung**, …) are **tree knots for browse** — not magically different datatypes by folder name. Product difference = **rules on the unit (and its dimension)**, not “Basiseinheit vs currency” as two hard-coded types. **Typical rule knobs:** prefix allowed? (allowlist; empty = off — currency/Stück); which units share a **dimension** (metre↔inch yes; Ohm↔Farad no); which **conversion engine** (SI/prefix factor, fixed UoM factor, FX rate+date). Meter↔inch and EUR↔GBP are the **same user story** (enter in foreign unit → convert to preferred) with **different engines**. **Storage open (Q121):** always canonical (m / EUR) vs store-as-entered — summing/rounding risk. Scaffold today still finds units by folder heuristics (`isBasiseinheitUnit`) — debt toward rule/meta + Q92 bindings. | decided |
| Q121 | Quantity / money **storage vs entry unit** (canonical always vs store-as-entered)? | Always canonical (m, EUR) / store entry triple only / hybrid | **Decided lean (2026-08-09):** **Money:** store **canonical EUR** (home currency) for sums/compare. On foreign entry (e.g. GBP): also persist **snapshot** `{ amount, currency, rate, date }` so the day’s purchase is auditable. Rounding once on book-in beats re-FX later. **Physical (m↔inch):** same canonical idea optional later; no daily rate — lower urgency; may follow same pattern without FX fields. Related Q109/Q110/Q119. | decided |
| Q122 | How do **type properties** work for plain types, inheritance, and **composed** types? | Type-only / hard-code per name / **uniform + dynamic components** | **Decided (2026-08-09):** (1) Every type has properties — set on the type node; **override along `child_of` inheritance** — same law **everywhere**. (2) **Composed** types (attributes / composition, e.g. `size` = Value + Unit): wherever you configure that type (type node, field typed as it, nested Options), you must reach the **component attributes’** property surfaces (Default, Preferred R/C/V, Choices/allowlists, …) **the same way** as when those attributes live on their type nodes. (3) Surfaces are **dynamic** from the live attribute/type graph (`Attribute::list` / `typeProperties`) — **never** hard-coded by type display name. Ties Q66/Q71/Q87/Q114/Q120; scaffold Festwert dialogs are debt until Registry Quantity/Unit defaults. | decided |
| Q123 | Attribute model: Relation vs slot; Settings walk; data vs view | Aux node / slots / **Relation-only + Settings.data/view** | **Decided (2026-08-09 OQ-W1…W16):** Relation-only attributes; recursive Settings/Render walk; hybrid overrides; `Settings.data` + `Settings.view`; diagrams in [`DEVELOPER-ATTRIBUTE-MODEL.md`](DEVELOPER-ATTRIBUTE-MODEL.md). Scaffold slots = debt. | decided |
| Q124 | RelationType **`defaultvalue_from`**: direction + when to refresh defaults from a provider attribute? | Consumer→provider / provider→consumer; **create/empty only** / live cascade on provider change | **Decided lean (2026-08-10) + open nuance; superseded shape by Q125 (2026-08-11):** Behaviour unchanged for seed: **From** = consumer host → **To** = provider host; **name** = attribute name; materialize create/empty; local default wins. **Scaffold ≈ `0.0.442`**. Live cascade still open. **Storage shape:** migrate to generic **`calc`** Relation + `op=default_from` (Q125) — alias `defaultvalue_from` until migrate. | decided |
| Q125 | Generic **calculation Relation** (`calc`) instead of one RelationType per behaviour? | Keep many RelationTypes / **one `calc` + `op` + optional props** | **Decided lean (2026-08-11):** One RelationType **`calc`** (UI DE: Berechnung). Required edge field **`op`**; optional **props** (`factor`, `offset`, `factor_ref`, …). First op: **`default_from`** (= today’s Q124). Later ops: `scale_factor`, `scale_ref`, `contains`, … Shared op *names* may align with Fuss `sum`/`avg` later, but aggregates stay on Fuss/`compute` until endpoints are clear. **SI Präfix stays leaf `multiplikator`** — not a `calc` edge. Scaffold ≈ **`0.0.456`**: seed `calc`, alias `defaultvalue_from`, BOM Bauart via `op=default_from`; Add picker hides legacy name. Detail: [`attribute-choice-inheritance.md`](plans/attribute-choice-inheritance.md). | decided |
| Q126 | How is the **node configuration page** composed so chrome stays one paint path? | Flat `renderDetail` / ad-hoc panels / **vertical ConfigPage box stack** | **Decided (2026-08-12; refined 2026-08-14 ≈ 0.0.532):** **`WTTConfigRender.renderPage(node, ctx)`** = only entry. Boxes: **Actions → Meta → IdentitySettings → Bools → Display → Attributes → Preview → Relations**. **No `childNodes` box** — Child-list Choices under Display when Preferred = `ChildListRenderer` (same for all types). Walk: Verdeckung + peek+1. Rule: [`.cursor/rules/config-renderers.mdc`](../.cursor/rules/config-renderers.mdc). | decided |
| OQ-A3 | How does attribute **Read-only** inherit / relate to node **Fixed value**? | RO inherits always / **default off, heir may switch on** / Fixed = lock | **Decided (2026-08-08):** RO **default off**; inheriting host may switch **on** (host `_wtt_attribute_readonly`). RO **replaces Fixed-as-lock**; syncs to slot `_wtt_readonly`. **Default value** (Festwert seed, UI “Default value”) is a **different job** — keep. Legacy slot `fixedEnabled` → treat as RO for paint; no mass-delete of fixed meta. Scaffold ≈ **`0.0.381`**. **Q115 (2026-08-09):** Settings UI drops Fixed-as-lock label; node `_wtt_fixed_*` = Default value; RO grayed outside slots. | decided |

## Typing vocabulary (closed)

Product typing uses **`_wtt_type_id` only**:

| Concept | SoT | Question it answers |
|---------|-----|---------------------|
| **`_wtt_type_id`** | Term meta | “**What is my type?**” Hierarchy: father via WP parent / `child_of` (Q88). Attributes: catalog type node (Q87). |
| **Chooser scope** | Q92 bindings (`chooser_root` / `chooser_focus`) | “**Which nodes may I pick as a field type?**” — no type-role flag; no folder-gate meta. |
| **Catalog lock** | `_wtt_is_template` + `_wtt_deletable` | Protected template nodes (Development-mode editable). |

Special branches/leaves the product must address → **settings / catalog bindings by `term_id`** (never by name at runtime).

**Registry builtins (Q96 decided):** `builtin.<registryId>` (e.g. `builtin.int`) in `wtt_catalog_bindings` → Simple/catalog term **id**. Lookup: type term id → key → Registry. Leaf **name** match = scaffold debt until migrate.

## Q34 / Q48 clarifiers

**Q34 decided (2026-08-08):** special *behavior* = **config** (+ Relations); not PHP subclass / hard kind. Typing = Q48/Q88/Q108/Q96.

### Q48 — types as Nodes + visible builtin key (ABC challenged)

| | |
|--|--|
| **Asks** | Where do scalars / catalog types live, and how does a slot bind to one? |
| **Agreed lean** | Types = **Nodes** in the Typ-Ast hierarchy; slots store **`_wtt_type_id`** (correct). |
| **Extra ask** | Hardcoded Registry behavior must be **visible** — not guessed from display name. |
| **Scaffold today** | `Case_Data::simple_datatype_leaves` / `complex_datatype_leaves`; Registry in `wtt-node-render.js` (`SIMPLE_SCALAR_KEYS`, `STRUCTURED_TYPE_KEYS`); media via `WTTMediaRender`; object hosts via `WTTObjectRender`. DTO/`typeKey` often = **name** — debt vs Q79/Q92. |

#### Inventory → three families

Sources: seed (`class-case-data.php`), Registry, media/set/quantity/table metas (`Node_Type`), Model hosts (out of “builtin ABC” scope).

| Family | Members (scaffold) | What differs |
|--------|--------------------|--------------|
| **Render-only** | `int`, `double`, `text`, `textarea`, `char`, `bool`, `email`; `node_presentation` (alias `display_node_name`) | Same Node shape; **widget/chrome** only (dedicated renderer). **`node_presentation` (≈ 0.0.490):** shows one Q117 host field via type/attr option (`form`/`table`/`select`/`symbol`/`help`/`icon`). **`int` (≈ 0.0.345):** one `IntRenderer` + Converter/validators; Number format UI on type + attribute override; format ids `arabic` (default) / `roman`/`binary`/`octal`/`hex` (reserved converters). |
| **Settings-bearing** | `media` (`allowed_kinds`, upload/url); `date` (`date_mode`); `set` (separator / joinUnits / labelChildren); `quantity` (+ unit allowlists / multiplikator on Basiseinheit); `node_ref` (`ref_scope`, allowed ids); legacy `node_embed` / `node_pick` (debt — Q72); legacy `table` (prop_bindings zeile/kopf/fuss, footer ops — **Q90 parked**) | Needs **typed config meta** (and sometimes structural bindings) **beyond** “which widget”. One string key is not the settings SoT. |
| **Attribute-bearing / composed** | Unit **set** = Typ+Praefix+Kuerzel (Q51/Q25); CatalogChoice hosts (Währung, Bauformen, Dioden Arten — **hierarchy**, Q90 depth); **Model hosts** (Kontakt, Platine, Bauteil kinds — attributes via Q87) | Fixed **slots / composition / specialization tree**, not a single renderer swap. Model hosts are **schema nodes**, not catalog simples — **out of scope** for “datatype ABC”. |

Parked Complex leaves **soft-trashed** from Fallstudie (≈ 0.0.463: `list` / `enum` / `node_*`); legacy `table` remains for BOM only — do not design ABC around extending them (Q90).

#### ABC vs families

| Option | Render-only | Settings-bearing | Attribute / composed |
|--------|-------------|------------------|----------------------|
| **A** — `implementationKey` meta only | **Works** — Registry dispatch + Meta chip; no name SoT for paint. | **Partial** — key picks renderer (`media`), but **settings stay separate metas**; alone cannot express allowlists/bands. Finding “the media catalog node” across installs without bindings → name debt unless every consumer already has `type_id`. | **Wrong tool** — composition/attrs are not an implementation key. |
| **B** — catalog bindings only (`builtin.text` → id) | **Awkward** — slot has `type_id`; paint needs key via **reverse** binding lookup (id→key) or hidden name. Visible “Builtin: text” needs that reverse map. | **Wrong layer** — bindings address **anchors**, not `allowed_kinds` / separators. Table `prop_bindings` are a *different* binding kind (instance structure), not `wtt_catalog_bindings`. | Anchors (Model, Data Types) already **Q92** — orthogonal to type widget identity. |
| **C** — binding + `implementationKey` | **Strong** for builtins — id SoT (Q92) + visible key + fast Registry resolve. Dual-write sync is the cost. | **Still incomplete** — C only answers “which builtin”; settings need **NodeConfig / type meta** regardless. Applying C as *the* answer for media/set/quantity **overclaims**. | Same as A/B: use composition + `type_id` + Q92 for hosts; not C. |

**Verdict:** Flat A/B/C is **too coarse**. C (or A + sparse `builtin.*` bindings) fits **render-only**. Settings-bearing need **implementationKey (renderer) + structured config SoT**. Attribute/composed need **edges / children**, not ABC.

#### Refined model (proposed — confirm with user)

```text
Slot ──type_id──► Type Node
                     │
                     ├─ implementationKey?  → Registry renderer (render-only + which chrome for settings types)
                     ├─ NodeConfig / type metas → media kinds, set separators, date_mode, ref_scope, …
                     ├─ composition / attributes (Q87) → set members, quantity usage, Model hosts
                     └─ catalog bindings (Q92) → anchors + optional builtin.* leaf ids (seed/migrate/product find)
```

| Layer | Job | SoT |
|-------|-----|-----|
| `type_id` on slot | “I am this type node” | Term meta `_wtt_type_id` |
| `implementationKey` | “Paint with this builtin renderer” | Term meta on **type** node (Dev-mode chip) |
| Type / NodeConfig metas | Settings-bearing extras | Existing `_wtt_*` (media, set, date, …) — do **not** encode as binding keys |
| Composition / attributes | Fixed slots / trinity / hosts | Children + `besteht_aus` / aggregation |
| `wtt_catalog_bindings` | Rename-safe **anchors** (+ optional `builtin.text` → id) | Option map key → term_id |
| Instance `prop_bindings` | Structural anchors inside one typed node (legacy table bands) | `_wtt_prop_bindings` — Rules/Fixes; not catalog ABC |

**Recommended lean (refined):** For **render-only builtins**, keep **C** (or A first, add `builtin.*` bindings when product must resolve leaves without a slot `type_id`). Do **not** pretend C replaces media/set/quantity config or Model attribute schemas.

#### Still open (user)

1. Confirm **split model** vs flat C-for-everything?
2. Ship **A first** (chip + Registry), add `builtin.*` bindings only where seed/tooling needs leaf ids?
3. Meta key name: `_wtt_implementation_key` vs reuse?
4. Abstract folders (`Simple`, `node_pick`): empty key?
5. Is **`date`** render-only + tiny `date_mode`, or full settings family?
6. Is **`quantity`** a builtin key, or always “composed object” (Preis-shaped) with no scalar renderer key?
7. Dual-write policy if C: binding wins on conflict / Dev-mode only edits key?

- **Q49 revised:** Simples **may** have specialization children (config presets); soft lean still no attrs-as-host / no outgoing Relations.
- **Q109 decided + scaffold ≈ 0.0.386:** quantity display triple; Präfix switch → **rescale Typ** (physical value constant); no cross-Basiseinheit silent switch.
- **Q110 parked (hold):** currency FX — not Q109 / not Q119 subunit scale.
- **Q119 decided lean:** money amount = precise major-unit store; ISO 4217 minor for entry scale; display via converters (adaptive A default); not Preis hard-code.
- **Q120 decided lean:** Quantity = Value+Prefix?+Unit; folders = knots; difference = **rules** (prefix/dimension/engine); meter↔inch ≈ EUR↔GBP story.
- **Q121 decided lean:** money → canonical EUR + foreign entry snapshot (amount/currency/rate/date); physical canonical later.
- **Q122 decided:** Type properties on the type + inheritance override everywhere; composed types expose **component attribute** settings dynamically (same surfaces as on those nodes); Festwert Registry defaults = scaffold debt. **Paint follow-on (≈ 0.0.558 / plan 0.7.140):** settings cascade detail→model; nested cells use type Preferred + Q117 `typePresentation` — no display-name hardcoding.
- **UX construction (open, 2026-08-09):** ~~Unit/prefix marriage power-user only~~ → **Allowed prefixes** panel on unit detail ≈ **`0.0.406`**. Attributes panel still eager (lean: collapsed+lazy like Relations ≈ `0.0.405`). Folder ID bindings for With/Without prefix optional later.
- **Q111 revised:** **Bindung** = storage (Composition → embedded in parent; Aggregation → linked Model). No host-type flag.
- **Q112 revised (≈ 0.0.546 / Bindung chrome ≈ 0.0.552 / kind Preferred ≈ 0.0.554 / Composition Display + Compact labels ≈ 0.0.555):** Preferred wire **`MultistepRenderer`**; UI **Multistep**; mode dialog|inline (≠ Q74). **Composition** = step fill in context + Display sync; **Aggregation** = list/search/create/bind (UR-B6). Compact: `_wtt_compact_show_labels`.
- **Q114 decided:** Attribute Options = same Node config chrome (R/C/V); Preferred render+converter side by side.

## Documentation backlog

| Item | Status | Note |
|------|--------|------|
| End-user **constellation recipes** (SI, `calc`/default_from, CatalogChoice heir, Money, own attrs) | **Backlog** | Stub + seed list: [`plans/user-constellation-recipes.md`](plans/user-constellation-recipes.md). Distinct from planning [`example-projects.md`](plans/example-projects.md). Fill when asked. |

## How to close a question

1. Record the choice in this table (`Status: decided` or `deferred`).
2. Add a dated entry to the project plan decision log.
3. Update `docs/ARCHITECTURE.md` / `docs/PRODUCT.md` / `docs/ROADMAP.md` if the answer changes scope or shape.
4. Do **not** implement code as part of closing the question while planning mode is active.
